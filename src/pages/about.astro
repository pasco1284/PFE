---
// Importer des composants
import Header from "./composants/Header.astro";
import Footer from "./composants/Footer.astro"; 
---

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/png" href="/icon.png" />
  <title>Plateforme de Services Éducatifs</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Roboto', sans-serif;
      background-image: url('/background.png');
      background-size: cover;
      background-attachment: fixed;
      background-repeat: no-repeat;
      background-position: center;
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      overflow-x: hidden;
    }

    #content {
      flex: 1;
      display: flex;
      justify-content: space-between;
      padding: 30px;
      flex-wrap: wrap;
      backdrop-filter: blur(5px);
      animation: fadeIn 1.5s ease-in-out;
    }

    .left-description {
      flex: 1;
      margin-right: 20px;
      background-color: rgba(255, 255, 255, 0.85);
      padding: 30px;
      border-left: 5px solid #007bff;
      border-radius: 10px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
      opacity: 0;
      transform: translateX(-80px);
      animation: slideInLeft 1.2s ease-out forwards;
    }

    .left-description h2 {
      margin-bottom: 15px;
      font-size: 2em;
      color: #007bff;
      font-weight: 700;
    }

    .left-description h3 {
      color: #0056b3;
      margin-top: 15px;
    }

    .left-description p {
      margin-top: 10px;
    }

    .right-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 20px;
      animation: floatUp 2s ease-in-out;
    }

    .service {
      background: rgba(255, 255, 255, 0.9);
      border-left: 5px solid #007bff;
      border-radius: 10px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
      padding: 25px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .service h2 {
      color: #007bff;
      font-family: 'Open Sans', sans-serif;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .service p {
      font-family: 'Roboto', sans-serif;
      font-weight: 400;
    }

    .service:hover {
      transform: translateY(-8px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }

    button {
      margin-top: 20px;
      padding: 12px 24px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    button:hover {
      background-color: #0056b3;
      transform: scale(1.05);
    }

    @keyframes slideInLeft {
      0% {
        opacity: 0;
        transform: translateX(-100px);
      }
      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
      }
      100% {
        opacity: 1;
      }
    }

    @keyframes floatUp {
      0% {
        transform: translateY(30px);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @media (max-width: 992px) {
      #content {
        flex-direction: column;
        padding: 20px;
      }

      .left-description,
      .right-section {
        width: 100%;
        margin-right: 0;
      }

      .left-description {
        animation: none;
        opacity: 1;
        transform: none;
      }
    }

    @media (max-width: 576px) {
      body {
        font-size: 14px;
      }

      .left-description,
      .service {
        padding: 20px;
      }

      .service h2 {
        font-size: 1.2em;
      }

      button {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <Header />
  <section id="content">
    <div class="left-description">
      <h2>Qui Sommes-Nous ?</h2>
      <p>Notre plateforme facilite la collaboration entre enseignants et étudiants en offrant des outils adaptés à l'apprentissage moderne.</p>
      <h3>Gestion de cours simplifiée :</h3>
      <p>Les enseignants peuvent facilement mettre en ligne leurs cours, ressources pédagogiques et devoirs.</p>
      <h3>Communication fluide :</h3>
      <p>Une messagerie instantanée intégrée permet une interaction rapide et efficace entre étudiants et enseignants.</p>
      <h3>Suivi de progression :</h3>
      <p>Chaque étudiant peut suivre ses progrès tout au long de son parcours éducatif, avec des mises à jour régulières sur ses performances et ses objectifs.</p>
      <p>Ensemble, nous favorisons une expérience d'apprentissage interactive et enrichissante.</p>
    </div>

    <div class="right-section">
      <div class="service">
        <h2>Export/Import de Fichiers</h2>
        <p>Notre plateforme offre une solution efficace pour l’échange de fichiers essentiels entre étudiants et enseignants.</p>
      </div>
      <div class="service">
        <h2>Chat Box</h2>
        <p>La Chat Box intégrée à notre plateforme est un outil puissant qui permet une communication fluide et instantanée entre les étudiants et les enseignants.</p>
      </div>
      <div class="service">
        <h2>Suivi des Progrès</h2>
        <p>Le Suivi des Progrès est une fonctionnalité incontournable de notre plateforme, conçue pour aider les étudiants à visualiser et à évaluer leur parcours académique.</p>
      </div>
    </div>
  </section>
  <Footer />
</body>
</html>
